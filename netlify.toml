[build]
  command = "npm run build"
  publish = "dist"

# SPA redirect — todas las rutas apuntan a index.html para que React Router funcione
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
